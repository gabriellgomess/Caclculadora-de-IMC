import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerResultImc:{
        marginTop: 30,
    },
    titleResultImc:{
        fontSize: 25,
        color: '#2a2a2a',
        textAlign: 'center',
    },
    textResult:{
        fontSize: 18,
        color: '#2a2a2a',
        textAlign: 'center',
        marginTop: 15,
    }
});

export default styles;